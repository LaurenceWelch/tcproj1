import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  header: {fontSize: 24, margin: 10, textAlign: 'center'},
  content: {fontSize: 20, margin: 4},
});

export default styles;
